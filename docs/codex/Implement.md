# Implementation Notes

- The app currently runs in a polished demo mode when Supabase public env vars are missing.
- GitHub publishing and Vercel linking are complete.
- Supabase local config was initialized and the exercise catalog source was pulled from `free-exercise-db`.
- The active workout flow persists in local storage so unfinished sessions recover across navigation.
