## 2024-05-24 - Improve search input type
**Learning:** Using `type="search"` instead of default text input natively triggers the iOS "Search" keyboard key, improving mobile UX significantly for search fields.
**Action:** Always verify search inputs have `type="search"` set instead of relying on default `type="text"`.
